akka {
    loglevel = "DEBUG"
    # log-config-on-start = on

    actor {
        debug {
            receive = on
            autoreceive = on
            lifecycle = on
            fsm = on
            event-stream = on
        }
    }
}
